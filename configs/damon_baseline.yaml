# Contact head on DAMON/DECO (per-vertex SMPL-6890). Thin override of base.yaml;
# all model / loss / optim values are the base defaults.
base: configs/base.yaml

data:
  datasets:
    - {name: damon, config: configs/datasets/damon.yaml, split: trainval}

output:
  exp_name: contact
